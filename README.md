# AI Coding Agent (SDLC Automation)

Автоматизированная система полного цикла разработки (SDLC), работающая внутри GitHub.
Проект реализует роли **AI Developer** и **AI Reviewer**, которые взаимодействуют через Issues, Pull Requests и Code Review.

## 🎥 Демонстрация работы
> (Сюда лучше всего вставить GIF или ссылку на закрытый Issue, где виден весь цикл: задача -> код -> критика -> фикс -> аппрув).
* **Пример Issue:** [ссылка]
* **Пример PR:** [ссылка]

## 🏗 Архитектура
Решение построено на **GitHub Actions** и использует API **OpenRouter (DeepSeek/GPT-4)**.

### Компоненты системы:
1.  **Code Agent (Developer):**
    * Читает Issue.
    * Анализирует файловую структуру проекта.
    * Пишет код на любом языке (Python, Go, JS и др.).
    * Создает PR или пушит исправления в существующую ветку.
2.  **Reviewer Agent:**
    * Анализирует Diff изменений в PR.
    * Проверяет статус CI (линтеры, тесты).
    * Пишет комментарии в PR.
    * Ставит вердикт: `LGTM` (Merge) или `Changes requested`.
3.  **Loop & Orchestration:**
    * Цикл исправлений запускается автоматически при наличии замечаний.
    * Реализована защита от бесконечных циклов (макс. 5 итераций).

## 🛠 Технический стек
* **Язык:** Python 3.11
* **LLM Integration:** LangChain + OpenRouter
* **Git:** PyGithub + Git CLI
* **Containerization:** Docker + Docker Compose

## 🚀 Как запустить локально

Для тестирования агента локально без GitHub Actions:

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repo_url>
   cd coding-ai-agent
   ```
2. **Настройте окружение: Создайте файл .env:**
   ```
   GH_PAT=ghp_your_token... (GitHub Personal access token (classic))
   OPENROUTER_API_KEY=sk-or-your_key... (OpenRouter API Key)
   GITHUB_REPOSITORY=username/repo_name
   ```
3. **Запуск через Docker Compose:**
   ```bash
   docker-compose up -d --build
   ```
4. **Ручной запуск сценариев (через CLI):**
   ```bash
   # Решить задачу из Issue #1
   docker-compose exec agent python coder.py --issue 1

   # Провести ревью PR #2
   docker-compose exec agent python reviewer.py --pr 2

   # Запустить фиксера для PR #2 (исправление ошибок)
   docker-compose exec agent python coder.py --pr 2 --fix
   ```
