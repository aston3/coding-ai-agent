# 🤖 AI Coding Agent (GitHub App Version)

Автономный AI-агент, который работает как ваш личный разработчик. Он интегрируется через **GitHub App**, слушает события через Webhooks и автоматически пишет код, проводит ревью и исправляет ошибки.

В отличие от GitHub Actions, этот агент работает на **вашем собственном сервере** (VPS), использует безопасную аутентификацию через GitHub App (JWT/Installation Tokens) и не требует сложной настройки YAML-файлов в каждом репозитории.

## Ссылка на установку моего GitHub App

https://github.com/apps/aston3-coding-ai-agent

## ✨ Возможности

* **🚀 AI Developer:** Создает новую ветку, пишет код и открывает Pull Request на основе описания в **Issue**.
* **🕵️ AI Reviewer:** Автоматически проверяет код в новых **Pull Requests**, анализирует изменения (Diff) и оставляет комментарии с замечаниями.
* **🛠 AI Fixer:** Исправляет код по запросу. Если вы (или AI-ревьюер) оставите комментарий с критикой, агент внесет правки и обновит PR.
* **🔄 Самоисцеление:** Если при генерации возникли ошибки, агент попытается их исправить самостоятельно (до 5 итераций).
* **🔐 Безопасность:** Работает через GitHub App. Использует временные токены доступа, которые истекают через час.
* **🧹 Чистота:** Для каждой задачи агент клонирует репозиторий в изолированную папку `repo/`, а после завершения работы — удаляет её.

---

## 🏗 Архитектура

Проект упакован в Docker-контейнер, который поднимает Flask-сервер.

1.  **GitHub** отправляет Webhook (событие) на ваш сервер при создании Issue, PR или комментария.
2.  **Server (`server.py`)** принимает сигнал и аутентифицируется как приложение (получает токен для конкретного репозитория).
3.  Запускается отдельный процесс (`coder.py` или `reviewer.py`).
4.  Агент клонирует репозиторий во временную директорию, выполняет задачу (через LLM), пушит изменения и удаляет временные файлы.

---

## 🛠 Установка и Деплой на своем облаке

Для работы вам понадобится сервер с установленным **Docker** и **Docker Compose**.

### Шаг 1. Создание GitHub App

1.  Перейдите в [Developer Settings -> GitHub Apps](https://github.com/settings/apps).
2.  Нажмите **New GitHub App**.
3.  **Homepage URL:** Ссылка на ваш профиль или репозиторий.
4.  **Webhook URL:** `http://<IP-ВАШЕГО-СЕРВЕРА>/webhook` (порт 80) или HTTPS адрес.
5.  **Permissions (Права доступа):**
    * `Repository` -> **Contents**: *Read and Write* (для кода).
    * `Repository` -> **Pull requests**: *Read and Write* (для PR и комментов).
    * `Repository` -> **Issues**: *Read and Write* (для чтения задач).
    * `Repository` -> **Workflows**: *Read and Write* (для обновления CI/CD).
6.  **Subscribe to events (События):**
    * ✅ `Issues`
    * ✅ `Pull request`
    * ✅ `Issue comment`
7.  Создайте приложение.
8.  **Скачайте Private Key** (`.pem` файл) внизу страницы.
9.  Скопируйте **App ID** (вверху страницы).

### Шаг 2. Запуск на сервере

1.  **Клонируйте этот репозиторий:**
    ```bash
    git clone -b cloud [https://github.com/aston3/coding-ai-agent.git](https://github.com/aston3/coding-ai-agent.git)
    cd coding-ai-agent
    ```

2.  **Настройте переменные окружения:**
    Создайте файл `.env`:
    ```ini
    # ID вашего приложения из GitHub
    GITHUB_APP_ID=123456

    # API Key для LLM (OpenRouter, OpenAI и т.д.)
    OPENROUTER_API_KEY=sk-or-your-key-here...

    # Модель (например, DeepSeek, Claude, GPT-4)
    MODEL_NAME=deepseek/deepseek-r1

    # Путь к ключу внутри контейнера (не меняйте)
    PRIVATE_KEY_PATH=/app/private-key.pem
    ```

3.  **Добавьте ключ:**
    Положите скачанный файл `private-key.pem` в корень папки проекта (рядом с `.env`).

4.  **Запустите:**
    ```bash
    sudo docker compose up -d --build
    ```

### Шаг 3. Установка приложения в репозитории

1.  Вернитесь в настройки вашего GitHub App -> **Install App**.
2.  Нажмите **Install** и выберите репозитории, к которым агент получит доступ.

---

## 🚀 Как использовать

### 1. Поставить задачу (New Feature)
Создайте **Issue** в подключенном репозитории.
* **Заголовок:** Краткая суть (например, `Add login page`).
* **Описание:** Подробное ТЗ.

🤖 *Агент создаст ветку `feature/issue-N`, напишет код и откроет Pull Request.*

### 2. Код-ревью
Просто откройте любой **Pull Request**.
🤖 *Агент проанализирует изменения и напишет комментарий с разбором ошибок или одобрением.*

### 3. Исправление (Fix)
Если в PR есть ошибки, напишите комментарий к PR:
* `Исправь ошибку в строке 10`
* `Fix formatting`

🤖 *Агент прочитает комментарий, внесет исправления и сделает `git push` в ту же ветку.*

---

## 📁 Структура проекта

* `server/` — Веб-сервер (Flask) для обработки Webhooks.
* `coder.py` — Логика написания кода и работы с Git.
* `reviewer.py` — Логика AI-ревьюера.
* `configs/` — Настройки промптов и инструментов.
* `repo/` — Временная папка (создается и удаляется автоматически).

## 🤝 Разработка

Для локального тестирования без белого IP используйте **ngrok**:
```bash
ngrok http 80
```
